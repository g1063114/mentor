spring:
  jpa:
    hibernate:
      ddl-auto-: create
    properties:
      hibernate:
        format_sql: true
        #use_sql_comments: true

    logging.level:
      org.hibernate.SQL: debug

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/mentoring?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: CJH21147556!!
